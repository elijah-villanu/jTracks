# Python
.venv/
__pycache__/
*.pyc
.pytest_cache/

# Local dev databases (SQLite)
*.db
jtracks_dev.db

# Env
# The real .env must never be committed; the template documents it and SHOULD be
# (audit L6 — these two lines used to ignore both).
.env
.env.*
!.env.example
