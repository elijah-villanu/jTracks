# --- Shared / database workstream (DATABASE_TASKS.md) ---
# Pinned upper bounds added by the backend workstream: fastapi 0.139+ pulls a
# starlette that requires the brand-new `httpx2`; pinning keeps the mature,
# well-documented stack the PRD asks for.
fastapi>=0.115,<0.116
uvicorn[standard]>=0.32
sqlalchemy>=2.0
alembic>=1.13
psycopg[binary]>=3.2
pydantic-settings>=2.5

# --- Backend workstream (BACKEND_TASKS.md) ---
starlette>=0.41,<0.42
python-multipart>=0.0.32
email-validator>=2.2
# Auth
python-jose[cryptography]>=3.3
bcrypt>=4.1
# Per-IP rate limiting on auth/autofill endpoints (SECURITY_AUDIT H4)
slowapi>=0.1.9
# Google OAuth ID-token verification
google-auth>=2.30
requests>=2.32
# Autofill (outbound HTTP); <0.28 to stay compatible with starlette TestClient
httpx>=0.27,<0.28
# In-process scheduler (see docs/decisions/scheduler-mechanism.md)
APScheduler>=3.10
# Tests
pytest>=8.0
